on:
  push:
    branches:
      - master
name: coverage
jobs:
  update-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: bcoe/release-please-action@master
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          release-type: node
