on:
  push:
    branches:
      - master
name: coverage
jobs:
  update-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: bcoe/release-please-action
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          release-type: node
